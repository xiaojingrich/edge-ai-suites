# Windows minimal runtime deps for vlm-openvino-serving (CPU or GPU via OpenVINO)
# Install with:
#   pip install -r requirements.windows.cpu.txt
#
# Notes:
# - OpenVINO does the inference. Selecting CPU vs GPU is done via env var `VLM_DEVICE`.
# - `torch` is kept CPU-only here to reduce install size; it is used for utilities/seeding.
# - `gunicorn` is not needed on Windows (use `uvicorn`).
# - `nncf` is not required for basic runtime (mainly for optional compression workflows).

--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.8.0
torchvision==0.23.0

fastapi==0.121.3
fastapi-utils==0.8.0
uvicorn==0.38.0

pydantic==2.11.7
pydantic-settings==2.10.1
typing-inspect==0.9.0

aiohttp==3.13.2
pillow==11.3.0
transformers==4.53.3

openvino==2025.3.0
openvino-tokenizers==2025.3.0.0
openvino-genai==2025.3.0.0
optimum-intel==1.26.0
qwen-vl-utils==0.0.11

numpy
pyyaml

